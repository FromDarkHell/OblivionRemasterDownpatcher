<ui:FluentWindow x:Class="OblivionRemasterDownpatcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OblivionRemasterDownpatcher"
        xmlns:pages="clr-namespace:OblivionRemasterDownpatcher.Pages"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Oblivion Remastered Downpatcher" 
        ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
        ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        ExtendsContentIntoTitleBar="True"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        WindowStartupLocation="CenterScreen"
        Width="1100"
        Height="650"
        d:DesignHeight="450"
        d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Frame Source="Pages/DashboardPage.xaml" Grid.Row="1"/>

        <ui:TitleBar
            Title="Oblivion Remaster Downpatcher"
            Grid.Row="0"
            Icon="pack://application:,,,/Assets/applicationIcon-256.png" />
    </Grid>
</ui:FluentWindow>
